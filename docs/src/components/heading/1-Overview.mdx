import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, BannerTabs, ComponentTabs, Footer, Lead } from '../../../.storybook/blocks'
import * as HeadingStories from './heading.stories'
import { HEADING_DOC_CONFIG, getHeadingTabs } from './heading.doc-config'

<Meta title="Components/Heading/Overview" />
<Banner label={'Overview'} section={HEADING_DOC_CONFIG.section} color={HEADING_DOC_CONFIG.color} />
<BannerTabs
  of={HeadingStories}
  tabs={getHeadingTabs('overview')}
/>

<Lead>
  **Heading** renders semantic HTML heading elements (h1–h6) with flexible styling options for visual hierarchy independent of markup level.
</Lead>

<Canvas of={HeadingStories.Basic} sourceState="shown" />

<ComponentTabs
  tabs={[
    {
      id: 'ds-heading',
      label: 'ds-heading',
      content: <Controls of={HeadingStories.Basic} />,
    },
  ]}
/>

<Footer />
