import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, BannerTabs, ComponentTabs, Footer, Lead } from '../../../.storybook/blocks'
import * as ContentStories from './content.stories'
import { CONTENT_DOC_CONFIG, getContentTabs } from './content.doc-config'

<Meta title="Components/Content/Overview" />
<Banner label={'Overview'} section={CONTENT_DOC_CONFIG.section} color={CONTENT_DOC_CONFIG.color} />
<BannerTabs
  of={ContentStories}
  tabs={getContentTabs('overview')}
/>

<Lead>
  **Content** is a layout and navigation container component. Provides a semantic wrapper for main content areas with optional sections, headers, and organized content flow management.
</Lead>

<Canvas of={ContentStories.Basic} sourceState="shown" />

<ComponentTabs
  tabs={[
    {
      id: 'ds-content',
      label: 'ds-content',
      content: <Controls of={ContentStories.Basic} />,
    },
  ]}
/>

<Footer />
