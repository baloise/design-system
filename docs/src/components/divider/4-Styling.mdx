import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  Footer,
  TokenOverview,
} from '../../../.storybook/blocks'
import * as DividerStories from './divider.stories'
import DividerParts from './divider.parts.svg'
import { DIVIDER_DOC_CONFIG, getDividerTabs } from './divider.doc-config'

<Meta title="Components/Divider/Styling" />
<Banner label={'Styling'} section={DIVIDER_DOC_CONFIG.section} color={DIVIDER_DOC_CONFIG.color} />
<BannerTabs
  of={DividerStories}
  tabs={getDividerTabs('styling')}
/>

## CSS Shadow Parts

Shadow DOM parts are named elements within a web component that can be targeted with CSS selectors from outside the component, allowing you to style specific internal elements without breaking encapsulation.

<br />

**Example:** Target the divider line:

```css
/* Style the divider line */
ds-divider::part(divider) {
  stroke-width: 2px;
  opacity: 0.8;
}
```

<br />

Primary parts of the divider component include:

<img src={DividerParts} alt="Divider parts diagram" />

<Markdown>
  {`
| Part | Element | Purpose |
| --- | --- | --- |
| \`divider\` | Line element | The visual separator line; can be horizontal or vertical depending on layout prop. |
`}</Markdown>

## Design Tokens

<TokenOverview component="divider" />

<Footer />
