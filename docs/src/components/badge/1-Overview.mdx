import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, BannerTabs, BasicStoryTabs, Footer, InfoQuote, Lead, StoryHeading, TokenOverview } from '../../../.storybook/blocks'
import * as BadgeStories from './badge.stories'
import BadgeParts from './badge.parts.svg'
import { BADGE_DOC_CONFIG, getBadgeTabs } from './badge.doc-config'

<Meta title="Components/Badge/Overview" />
<Banner label={'Overview'} section={BADGE_DOC_CONFIG.section} color={BADGE_DOC_CONFIG.color} />
<BannerTabs
  of={BadgeStories}
  tabs={getBadgeTabs('overview')}
/>

<Lead>
  **Badge** displays a small indicator or counter on a child component to highlight notifications, counts, or status information.
</Lead>

<Canvas of={BadgeStories.Basic} sourceState="shown" />

<Controls of={BadgeStories.Basic} />

<Footer />
