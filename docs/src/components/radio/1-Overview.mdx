import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, BannerTabs, ComponentTabs, Footer, Lead } from '../../../.storybook/blocks'
import * as RadioStories from './radio.stories'
import { RADIO_DOC_CONFIG, getRadioTabs } from './radio.doc-config'

<Meta title="Components/Forms/Radio/Overview" />
<Banner label={'Overview'} section={RADIO_DOC_CONFIG.section} color={RADIO_DOC_CONFIG.color} />
<BannerTabs
  of={RadioStories}
  tabs={getRadioTabs('overview')}
/>

<Lead>
  **Radio** is a form control for selecting a single option from a group of mutually exclusive choices. Use radio buttons when users should select exactly one option from a small set of alternatives.
</Lead>

<Canvas of={RadioStories.Basic} sourceState="shown" />

<ComponentTabs
  tabs={[
    {
      id: 'ds-radio',
      label: 'ds-radio',
      content: <Controls of={RadioStories.Basic} />,
    },
  ]}
/>

<Footer />
