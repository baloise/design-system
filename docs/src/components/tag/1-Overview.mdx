import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  ComponentPublicMethods,
  ComponentTabs,
  ComponentLead,
  Footer,
} from '../../../.storybook/blocks'
import * as TagStories from './tag.stories'
import { TAG_DOC_CONFIG, getTagTabs } from './tag.doc-config'

<Meta title="Components/Tag/Overview" />

<Banner label={'Overview'} section={TAG_DOC_CONFIG.section} color={TAG_DOC_CONFIG.color} />

<BannerTabs of={TagStories} tabs={getTagTabs('overview')} />

<ComponentLead component="tag" />

<Canvas of={TagStories.Basic} sourceState="shown" />

<ComponentTabs
  tabs={[
    {
      id: 'ds-tag',
      label: 'ds-tag',
      content: <Controls of={TagStories.Basic} />,
    },
  ]}
/>

<ComponentPublicMethods component="tag" />

<Footer />
