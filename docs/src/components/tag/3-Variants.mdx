import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  CanvasTabs,
  Footer,
  StoryHeading,
} from '../../../.storybook/blocks'
import * as TagStories from './tag.stories'
import { TAG_DOC_CONFIG, getTagTabs } from './tag.doc-config'

<Meta title="Components/Tag/Variants/Overview" />

<StoryHeading of={TagStories.Basic} hidden></StoryHeading>

<Banner label={'Variants'} section={TAG_DOC_CONFIG.section} color={TAG_DOC_CONFIG.color} />

<BannerTabs  of={TagStories}  tabs={getTagTabs('variants')}/>

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={TagStories.Closable}></StoryHeading>

Closable tags that can be removed by the user, providing a way to manage and dismiss tags as needed.

<CanvasTabs htmlOf={TagStories.ClosableHtml} of={TagStories.Closable} />

{/* ------------------------------------------------------ */}

<StoryHeading of={TagStories.TagGroup}></StoryHeading>

Grouping related tags together using the `TagGroup` component for better organization and visual hierarchy.

<CanvasTabs htmlOf={TagStories.TagGroupHtml} of={TagStories.TagGroup} />

{/* ------------------------------------------------------ */}

<StoryHeading of={TagStories.Colors}></StoryHeading>

Use `color` property to apply different color variants to the tag, indicating various statuses or categories.

<CanvasTabs htmlOf={TagStories.ColorsHtml} of={TagStories.Colors} />

<Footer />
