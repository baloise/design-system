import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, BannerTabs, CanvasTabs, Footer, StoryHeading } from '../../../.storybook/blocks'
import * as NotificationStories from './notification.stories'
import { NOTIFICATION_DOC_CONFIG, getNotificationTabs } from './notification.doc-config'

<Meta title="Components/Notification/Variants/Overview" />

<StoryHeading of={NotificationStories.Basic} hidden></StoryHeading>

<Banner label={'Variants'} section={NOTIFICATION_DOC_CONFIG.section} color={NOTIFICATION_DOC_CONFIG.color} />

<BannerTabs of={NotificationStories} tabs={getNotificationTabs('variants')} />

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={NotificationStories.Alerts}></StoryHeading>

With `alert` variant, the notification is styled to indicate an important message that requires user attention.
It typically uses a more prominent color scheme and may include an icon to signify urgency or importance.

<Canvas of={NotificationStories.Alerts} />

{/* ------------------------------------------------------ */}

<StoryHeading of={NotificationStories.Outlines}></StoryHeading>

With `outline` variant, the notification is styled with a border and a less prominent background,
making it suitable for less urgent messages or secondary information.

<Canvas of={NotificationStories.Outlines} />

<Footer />
