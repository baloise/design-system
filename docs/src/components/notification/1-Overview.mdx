import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, BannerTabs, ComponentTabs, Footer, Lead } from '../../../.storybook/blocks'
import * as NotificationStories from './notification.stories'
import { NOTIFICATION_DOC_CONFIG, getNotificationTabs } from './notification.doc-config'

<Meta title="Components/Notification/Overview" />
<Banner label={'Overview'} section={NOTIFICATION_DOC_CONFIG.section} color={NOTIFICATION_DOC_CONFIG.color} />
<BannerTabs
  of={NotificationStories}
  tabs={getNotificationTabs('overview')}
/>

<Lead>
  **Notification** displays inline feedback messages to inform, alert, or warn users about system events, actions, or status changes. Supports multiple severity levels with distinct visual styles.
</Lead>

<Canvas of={NotificationStories.Basic} sourceState="shown" />

<ComponentTabs
  tabs={[
    {
      id: 'ds-notification',
      label: 'ds-notification',
      content: <Controls of={NotificationStories.Basic} />,
    },
  ]}
/>

<Footer />
