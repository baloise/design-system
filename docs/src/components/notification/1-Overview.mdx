import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  BasicStoryTabs,
  ComponentPublicMethods,
  ComponentTabs,
  ComponentLead,
  Footer,
} from '../../../.storybook/blocks'
import * as NotificationStories from './notification.stories'
import { NOTIFICATION_DOC_CONFIG, getNotificationTabs } from './notification.doc-config'

<Meta title="Components/Notification/Overview" />

<Banner label={'Overview'} section={NOTIFICATION_DOC_CONFIG.section} color={NOTIFICATION_DOC_CONFIG.color} />

<BannerTabs of={NotificationStories} tabs={getNotificationTabs('overview')} />

<ComponentLead component="notification" />

<BasicStoryTabs tag="button" htmlStory={NotificationStories.BasicHtml} webComponentStory={NotificationStories.Basic} index={1} noGuide />

<ComponentTabs
  tabs={[
    {
      id: 'ds-notification',
      label: 'ds-notification',
      content: <Controls of={NotificationStories.Basic} />,
    },
  ]}
/>

<ComponentPublicMethods component="notification" />

<Footer />
