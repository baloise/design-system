import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  BasicStoryTabs,
  Footer,
  StoryHeading,
} from '../../../.storybook/blocks'
import * as CloseStories from './close.stories'
import { CLOSE_DOC_CONFIG, getCloseTabs } from './close.doc-config'

<Meta title="Components/Close/Variants/Overview" />
<StoryHeading of={CloseStories.Basic} hidden></StoryHeading>
<Banner label={'Variants'} section={CLOSE_DOC_CONFIG.section} color={CLOSE_DOC_CONFIG.color} />
<BannerTabs
  of={CloseStories}
  tabs={getCloseTabs('variants')}
/>

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={CloseStories.Sizes}></StoryHeading>

Close buttons are available in multiple sizes. Choose a size that provides adequate click target while maintaining visual hierarchy.

<BasicStoryTabs tag="close" noGuide />

{/* ------------------------------------------------------ */}

<StoryHeading of={CloseStories.States}></StoryHeading>

Close buttons support disabled state to prevent interaction when dismissal is not available.

<BasicStoryTabs tag="close" noGuide />

<Footer />
