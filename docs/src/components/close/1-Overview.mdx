import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, BannerTabs, ComponentTabs, Footer, Lead } from '../../../.storybook/blocks'
import * as CloseStories from './close.stories'
import { CLOSE_DOC_CONFIG, getCloseTabs } from './close.doc-config'

<Meta title="Components/Close/Overview" />
<Banner label={'Overview'} section={CLOSE_DOC_CONFIG.section} color={CLOSE_DOC_CONFIG.color} />
<BannerTabs
  of={CloseStories}
  tabs={getCloseTabs('overview')}
/>

<Lead>
  **Close** is a small button component designed to dismiss or remove elements. Perfect for closing modals, notifications, tags, and other dismissible UI elements.
</Lead>

<Canvas of={CloseStories.Basic} sourceState="shown" />

<ComponentTabs
  tabs={[
    {
      id: 'ds-close',
      label: 'ds-close',
      content: <Controls of={CloseStories.Basic} />,
    },
  ]}
/>

<Footer />
