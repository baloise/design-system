import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  Footer,
  TokenOverview,
} from '../../../.storybook/blocks'
import * as InputStories from './input.stories'
import { INPUT_DOC_CONFIG, getInputTabs } from './input.doc-config'

<Meta title="Components/Forms/Input/Styling" />
<Banner label={'Styling'} section={INPUT_DOC_CONFIG.section} color={INPUT_DOC_CONFIG.color} />
<BannerTabs
  of={InputStories}
  tabs={getInputTabs('styling')}
/>

## CSS Shadow Parts

Shadow DOM parts are named elements within a web component that can be targeted with CSS selectors from outside the component, allowing you to style specific internal elements without breaking encapsulation.

<br />

**Example:** Target the input field:

```css
/* Style the input element */
ds-input::part(input) {
  border-radius: 4px;
  padding: 12px;
}
```

<br />

Primary parts of the input component include:

| Part | Element | Purpose |
| --- | --- | --- |
| `input` | Input | The native input element. |

## Design Tokens

CSS variables (custom properties) allow you to define reusable values that can be customized globally or per component instance to override default styling without modifying the component code.

<br />

**Example:** Override input styling:

```css
/* Override globally */
:root {
  --ds-input-color-background: #ffffff;
  --ds-input-color-border: #cccccc;
}

/* Override for a specific input */
ds-input {
  --input-color-background: #f5f5f5;
}
```

<TokenOverview component="input" />

<Footer />
