import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  BasicStoryTabs,
  Footer,
  StoryHeading,
} from '../../../.storybook/blocks'
import * as InputStories from './input.stories'
import { INPUT_DOC_CONFIG, getInputTabs } from './input.doc-config'

<Meta title="Components/Forms/Input/Variants/Overview" />
<StoryHeading of={InputStories.Basic} hidden></StoryHeading>
<Banner label={'Variants'} section={INPUT_DOC_CONFIG.section} color={INPUT_DOC_CONFIG.color} />
<BannerTabs
  of={InputStories}
  tabs={getInputTabs('variants')}
/>

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={InputStories.Types}></StoryHeading>

Inputs support various types including text, email, password, search, and more. Each type provides appropriate keyboard and validation behavior.

<BasicStoryTabs tag="input" noGuide />

{/* ------------------------------------------------------ */}

<StoryHeading of={InputStories.States}></StoryHeading>

Inputs can show validation states: valid, invalid, or neutral. Use these to provide user feedback.

<BasicStoryTabs tag="input" noGuide />

{/* ------------------------------------------------------ */}

<StoryHeading of={InputStories.WithIcon}></StoryHeading>

Inputs can display leading or trailing icons to provide context or indicate action (search, clear, etc).

<BasicStoryTabs tag="input" noGuide />

{/* ------------------------------------------------------ */}

<StoryHeading of={InputStories.Sizes}></StoryHeading>

Control input field size with size prop. Options include small and normal (default).

<BasicStoryTabs tag="input" noGuide />

<Footer />
