import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, Footer, InfoQuote, Lead, PlaygroundBar, StoryHeading, BasicStoryTabs, TokenOverview } from '../../../.storybook/blocks'
import * as DividerStories from './bal-divider.stories'

<Meta of={DividerStories} />

<StoryHeading of={DividerStories.Basic} hidden></StoryHeading>

<Banner of={DividerStories} />

<Lead>
  A **divider** is a slender line that serves to group content within stacks and layouts. The divider separates and
  differentiate sections of content or groups of menu items.
</Lead>

<br />
<br />

We recommend using standard <b>HTML elements</b> such as `<hr>` whenever possible instead of web
components. This helps improve the rendering speed of your application or website.

<BasicStoryTabs tag="divider" htmlStory={DividerStories.Basic} webComponentStory={DividerStories.WebComponentBasic} />

<Canvas of={DividerStories.Basic} sourceState="shown" />

<PlaygroundBar of={DividerStories.Basic}></PlaygroundBar>

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={DividerStories.Vertical}></StoryHeading>

Change the layout of the divider to vertical to use them in a horizontal stack component.

<Canvas of={DividerStories.Vertical} />

{/* ------------------------------------------------------ */}

<StoryHeading of={DividerStories.Dashed}></StoryHeading>

To divide inner section the dashed version can be used.

<Canvas of={DividerStories.Dashed} />

{/* ------------------------------------------------------ */}

## Component API

import api from './api.md?raw'

<Markdown>{api}</Markdown>

## CSS Variables

<TokenOverview component="divider" />

## Integration

import integration from '../../snippets/integration.md?raw'

<Markdown>{integration}</Markdown>

<Footer />
