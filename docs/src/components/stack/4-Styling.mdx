import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  Footer,
  TokenOverview,
} from '../../../.storybook/blocks'
import * as StackStories from './stack.stories'
import StackParts from './stack.parts.svg'
import { STACK_DOC_CONFIG, getStackTabs } from './stack.doc-config'

<Meta title="Components/Stack/Styling" />
<Banner label={'Styling'} section={STACK_DOC_CONFIG.section} color={STACK_DOC_CONFIG.color} />
<BannerTabs
  of={StackStories}
  tabs={getStackTabs('styling')}
/>

## CSS Shadow Parts

Shadow DOM parts are named elements within a web component that can be targeted with CSS selectors from outside the component, allowing you to style specific internal elements without breaking encapsulation.

<br />

**Example:** Target the stack container:

```css
/* Style the stack container */
ds-stack::part(stack) {
  border: 1px solid #ccc;
  border-radius: 8px;
  background: #fafafa;
}
```

<br />

Primary parts of the stack component include:

<img src={StackParts} alt="Stack parts diagram" />

<Markdown>
  {`
| Part | Element | Purpose |
| --- | --- | --- |
| \`stack\` | Container | The flex container that manages layout, spacing, and alignment of child elements. |
`}</Markdown>

## Design Tokens

<TokenOverview component="stack" />

<Footer />
