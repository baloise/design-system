import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  ComponentPublicMethods,
  ComponentTabs,
  ComponentLead,
  Footer,
} from '../../../.storybook/blocks'
import * as ListStories from './list.stories'
import { LIST_DOC_CONFIG, getListTabs } from './list.doc-config'

<Meta title="Components/List/Overview" />

<Banner label={'Overview'} section={LIST_DOC_CONFIG.section} color={LIST_DOC_CONFIG.color} />

<BannerTabs of={ListStories} tabs={getListTabs('overview')} />

<ComponentLead component="list" />

<Canvas of={ListStories.Basic} sourceState="shown" />

<ComponentTabs
  tabs={[
    {
      id: 'ds-list',
      label: 'ds-list',
      content: <Controls of={ListStories.Basic} />,
    },
  ]}
/>

<ComponentPublicMethods component="list" />

<Footer />
