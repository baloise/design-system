import { Meta } from '@storybook/addon-docs/blocks'
import { Banner, BannerTabs, CanvasTabs, CanvasWithCodePen, Footer, StoryHeading } from '../../../.storybook/blocks'
import * as SelectStories from './select.stories'
import { SELECT_DOC_CONFIG, getSelectTabs } from './select.doc-config'

<Meta title="Components/Select/Variants/Overview" />

<StoryHeading of={SelectStories.Basic} hidden></StoryHeading>

<Banner label={'Variants'} section={SELECT_DOC_CONFIG.section} color={SELECT_DOC_CONFIG.color} />

<BannerTabs of={SelectStories} tabs={getSelectTabs('variants')} />

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={SelectStories.Basic}></StoryHeading>

Default single select with a label and description. The web component enhances a native `<select>` via Slim Select; a plain HTML/CSS equivalent is available when JavaScript enhancement isn't needed.

<CanvasTabs htmlOf={SelectStories.BasicHtml} of={SelectStories.Basic} />

{/* ------------------------------------------------------ */}

<StoryHeading of={SelectStories.Searchable}></StoryHeading>

A search input inside the dropdown lets users filter options by typing.

<CanvasWithCodePen of={SelectStories.Searchable} sourceState="shown" />

{/* ------------------------------------------------------ */}

<StoryHeading of={SelectStories.Clearable}></StoryHeading>

A clear button resets the selection back to `null`.

<CanvasWithCodePen of={SelectStories.Clearable} sourceState="shown" />

{/* ------------------------------------------------------ */}

<StoryHeading of={SelectStories.Multiple}></StoryHeading>

Multiple selection allows users to pick several values at once.

<CanvasWithCodePen of={SelectStories.Multiple} sourceState="shown" />

{/* ------------------------------------------------------ */}

<StoryHeading of={SelectStories.Grouped}></StoryHeading>

Options can be organized under labeled groups with `optionGroups`.

<CanvasWithCodePen of={SelectStories.Grouped} sourceState="shown" />

<Footer />
