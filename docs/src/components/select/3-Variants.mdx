import { Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  CanvasWithCodePen,
  Footer,
} from '../../../.storybook/blocks'
import * as SelectStories from './select.stories'
import { SELECT_DOC_CONFIG, getSelectTabs } from './select.doc-config'

<Meta title="Components/Select/Variants/Overview" />

<Banner label={'Select'} section={SELECT_DOC_CONFIG.section} color={SELECT_DOC_CONFIG.color} />

<BannerTabs of={SelectStories} tabs={getSelectTabs('variants')} />

## Basic

Default select component with label and description.

<CanvasWithCodePen of={SelectStories.Basic} sourceState="shown" />

## Disabled

Disabled state prevents user interaction and indicates the field is not editable.

<CanvasWithCodePen of={SelectStories.Disabled} sourceState="shown" />

## Invalid

Invalid state shows validation errors and requires user correction.

<CanvasWithCodePen of={SelectStories.Invalid} sourceState="shown" />

<Footer />
