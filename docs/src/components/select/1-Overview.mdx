import { Controls, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  CanvasWithCodePen,
  ComponentLead,
  ComponentPublicMethods,
  ComponentCssVariants,
  Footer,
} from '../../../.storybook/blocks'
import * as SelectStories from './select.stories'
import { SELECT_DOC_CONFIG, getSelectTabs } from './select.doc-config'

<Meta title="Components/Select/Select" />

<Banner label={'Select'} section={SELECT_DOC_CONFIG.section} color={SELECT_DOC_CONFIG.color} />

<BannerTabs of={SelectStories} tabs={getSelectTabs('overview')} />

<ComponentLead component="select" />

<CanvasWithCodePen of={SelectStories.Basic} sourceState="shown" />

<Controls of={SelectStories.Basic} />

{/* <ComponentPublicMethods component="select" /> */}
<ComponentCssVariants component="select" />

<Footer />
