import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  ComponentPublicMethods,
  ComponentTabs,
  ComponentLead,
  Footer,
} from '../../../.storybook/blocks'
import * as CardStories from './card.stories'
import { CARD_DOC_CONFIG, getCardTabs } from './card.doc-config'

<Meta title="Components/Card/Overview" />

<Banner label={'Overview'} section={CARD_DOC_CONFIG.section} color={CARD_DOC_CONFIG.color} />

<BannerTabs of={CardStories} tabs={getCardTabs('overview')} />

<ComponentLead component="card" />

<Canvas of={CardStories.Basic} sourceState="shown" />

<ComponentTabs
  tabs={[
    {
      id: 'ds-card',
      label: 'ds-card',
      content: <Controls of={CardStories.Basic} />,
    },
  ]}
/>

<ComponentPublicMethods component="card" />

<Footer />
