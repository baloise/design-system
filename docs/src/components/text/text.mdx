import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, BasicStoryTabs, Footer, InfoQuote, Lead, PlaygroundBar, StoryHeading, TokenOverview } from '../../../.storybook/blocks'
import * as TextStories from './text.stories'

<Meta of={TextStories} />

<StoryHeading of={TextStories.Basic} hidden></StoryHeading>

<Banner of={TextStories} />

<Lead>**Text** is a fundamental component used to display and present textual content.</Lead>

<br />
<br />

We recommend using standard <b>HTML elements</b> such as `<p>`, `<span>`, `<b>`, and `<strong>` whenever possible instead of web
components. This helps improve the rendering speed of your application or website.

<BasicStoryTabs tag="heading" htmlStory={TextStories.Basic} webComponentStory={TextStories.WebComponentBasic} />

<PlaygroundBar of={TextStories.Basic}></PlaygroundBar>

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={TextStories.BoldText}></StoryHeading>

<Canvas of={TextStories.BoldText} sourceState="shown" />

{/* ------------------------------------------------------ */}

<StoryHeading of={TextStories.Colors}></StoryHeading>

<Canvas of={TextStories.Colors} sourceState="shown" />

{/* ------------------------------------------------------ */}

<StoryHeading of={TextStories.Sizes}></StoryHeading>

<Canvas of={TextStories.Sizes} sourceState="shown" />

{/* ------------------------------------------------------ */}

<StoryHeading of={TextStories.Spacing}></StoryHeading>

<Canvas of={TextStories.Spacing} sourceState="shown" />

{/* ------------------------------------------------------ */}

<StoryHeading of={TextStories.NestedText}></StoryHeading>

<Canvas of={TextStories.NestedText} sourceState="shown" />

{/* ------------------------------------------------------ */}

<StoryHeading of={TextStories.FontFamily}></StoryHeading>

<Canvas of={TextStories.FontFamily} sourceState="shown" />

{/* ------------------------------------------------------ */}

<StoryHeading of={TextStories.FontWeight}></StoryHeading>

<Canvas of={TextStories.FontWeight} sourceState="shown" />

{/* ------------------------------------------------------ */}

<StoryHeading of={TextStories.NoWrap}></StoryHeading>

If the text is longer than the container with the property `no-wrap` the text will be cut.

<Canvas of={TextStories.NoWrap} sourceState="shown" />

{/* ------------------------------------------------------ */}

## Component API

import api from './api.md?raw'

<Markdown>{api}</Markdown>

## CSS Variables

<TokenOverview component="text" />

## Integration

import integration from '../../snippets/integration.md?raw'

<Markdown>{integration}</Markdown>

<Footer />
