import { Canvas, Controls, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  CanvasTabs,
  ComponentPublicMethods,
  ComponentTabs,
  ComponentLead,
  Footer,
} from '../../../.storybook/blocks'
import * as TextStories from './text.stories'
import { TEXT_DOC_CONFIG, getTextTabs } from './text.doc-config'

<Meta title="Components/Text/Overview" />

<Banner label={'Overview'} section={TEXT_DOC_CONFIG.section} color={TEXT_DOC_CONFIG.color} />

<BannerTabs of={TextStories} tabs={getTextTabs('overview')} />

<ComponentLead component="text" />

<CanvasTabs of={TextStories.Basic} htmlOf={TextStories.BasicHtml} />

<ComponentTabs
  tabs={[
    {
      id: 'ds-text',
      label: 'Text',
      content: <Controls of={TextStories.Basic} />,
    },
  ]}
/>

<ComponentPublicMethods component="text" />

<Footer />
