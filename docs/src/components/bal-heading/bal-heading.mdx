import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, Footer, InfoQuote, Lead, PlaygroundBar, StoryHeading } from '../../../.storybook/blocks'
import * as HeadingStories from './bal-heading.stories'

<Meta of={HeadingStories} />

<StoryHeading of={HeadingStories.Basic} hidden></StoryHeading>

<Banner of={HeadingStories} />

<Lead>
  **Heading** is a key component for displaying important titles and headers to organize and emphasize content.
</Lead>

{/* <InfoQuote>
<p class='heading is-level-3'>Style import</p>

  We recommend using standard <b>HTML elements</b> such as `<h1>`, `<h2>`, `<h3>`, and `<h4>` whenever possible instead of web
  components. This helps improve the rendering speed of your application or website.

To import only the styles required for this component:

```css
@import '@baloise/ds-styles/css/components/heading.css';
```

Or import all components styles:

```css
@import '@baloise/ds-styles/css/components/all.css';
```

</InfoQuote> */}

<InfoQuote>
<p class='heading is-level-3'>Style import</p>

  We recommend using standard <b>HTML elements</b> such as `<h1>`, `<h2>`, `<h3>`, and `<h4>` whenever possible instead of web
  components. This helps improve the rendering speed of your application or website.

These styles are included in the core styles:

```css
@import '@baloise/ds-styles/css/core.css';
```

</InfoQuote>

<Canvas of={HeadingStories.Basic} sourceState="shown" />

<PlaygroundBar of={HeadingStories.Basic}></PlaygroundBar>

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.Levels}></StoryHeading>

<Canvas of={HeadingStories.Levels} sourceState={'shown'} />

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.Sizes}></StoryHeading>

<Canvas of={HeadingStories.Sizes} sourceState={'shown'} />

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.Colors}></StoryHeading>

<Canvas of={HeadingStories.Colors} sourceState={'shown'} />

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.NoWrap}></StoryHeading>

<Canvas of={HeadingStories.NoWrap} sourceState={'shown'} />

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.Spacing}></StoryHeading>

<Canvas of={HeadingStories.Spacing} sourceState={'shown'}/>

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.WebComponent}></StoryHeading>

<Canvas of={HeadingStories.WebComponent} sourceState={'shown'}/>

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.AutoLevel}></StoryHeading>

In some place in your app the heading requires a line break.
Using `auto-level`, one can specify the minimum visual level at which the heading can dynamically shrink.

<Canvas of={HeadingStories.AutoLevel} />

{/* ------------------------------------------------------ */}

### Component API

import api from './api.md?raw'

<Markdown>{api}</Markdown>

## Integration

import integration from '../../snippets/integration.md?raw'

<Markdown>{integration}</Markdown>

import theming from './theming.md?raw'

<Markdown>{theming}</Markdown>



<Footer />
