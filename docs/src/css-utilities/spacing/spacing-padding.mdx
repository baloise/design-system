import { Meta } from '@storybook/addon-docs/blocks'
import * as SpacingPaddingStories from './spacing-padding.stories'
import {
  Banner,
  CanvasWithCodePen,
  CssResponsiveTable,
  CssSpacingPadding,
  Footer,
  Lead,
  LinkCard,
  LinkCards,
} from '../../../.storybook/blocks'

<Meta
  title="CSS Utilities/Spacing/Padding"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

<Banner label="Padding" section="CSS Utilities" color="red" />

<Lead>Determines the space between the content and its border.</Lead>

## Classes

Spacing can be set with **padding** `p*` for all directions:

- `*t` for **top**
- `*r` for **right**
- `*b` for **bottom**
- `*l` for **left**
- `*x` **horizontally** for both left and right
- `*y` **vertically** for both top and bottom

<CssSpacingPadding />

## Single Side Padding

Padding can be defined for a specific edge.

<CanvasWithCodePen of={SpacingPaddingStories.SingleSidePadding} />

## Horizontal Padding

Shorthand classes allow you to define the same padding value for both the left and right sides.

<CanvasWithCodePen of={SpacingPaddingStories.HorizontalPadding} />

## Vertical Padding

The same padding value can also be defined at the top and bottom sides using shorthand classes.

<CanvasWithCodePen of={SpacingPaddingStories.VerticalPadding} />

## Responsive

Tailored responsive options are at your disposal for customizations according to screen size.
Simply append a responsive breakpoint keyword with a dash, like `tablet:p-large`, to apply the corresponding responsive class.

<CssResponsiveTable />

<Footer>
  <LinkCards>
    <LinkCard
      pageTitle="Foundation/Spacing"
      description="Global spacings help us design experiences that scale with consistency, rhythm, and focus"
    />
    <LinkCard
      pageTitle="Tokens/All Tokens"
      description="Design tokens serve as the definitive source for naming and storing design choices."
    />
  </LinkCards>
</Footer>
