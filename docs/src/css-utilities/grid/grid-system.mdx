import { Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, CanvasWithCodePen, CssResponsiveTable, Footer, Lead, LinkCard, LinkCards } from '../../../.storybook/blocks'
import * as GridSystemStories from './grid-system.stories'

<Meta
  title="CSS Utilities/Grid/Grid System"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

<Banner label="Grid System" section="CSS Utilities" color="red" />

<Lead>Determines how flex lines are distributed between and around items.</Lead>

## Basic

The grid is based on 12 grid. On mobile the grid will use the full width.

<CanvasWithCodePen of={GridSystemStories.Basic} />

## Column Sizes

The grid supports 12 different sizes of grid. They can be used with the number `is-12` or with words like `is-full`.

<CanvasWithCodePen of={GridSystemStories.ColumnSizes} />

## Rows

By adding the CSS class `is-multiline` the grid can be shown on more than one row.

<CanvasWithCodePen of={GridSystemStories.Rows} />

## Nested

Grids can be nested to have more flexibility in your structure.

Just keep the structure grid > column > grid > column > ...

<CanvasWithCodePen of={GridSystemStories.Nested} />

## Space

By default add a gap `var(--ds-column-gap)` of 1rem between the grid.
This can be avoided with setting the CSS class `is-gapless`.

<CanvasWithCodePen of={GridSystemStories.Space} />

## Vertical Alignment

To center the grid of the grid vertically set `is-vcentered` CSS class.

<CanvasWithCodePen of={GridSystemStories.VerticalAlignment} />

## Horizontal Alignment

To center the grid of the grid set `is-centered` CSS class.

<CanvasWithCodePen of={GridSystemStories.HorizontalAlignment} />

## Stretch

To stretch the inner element of a column use the CSS class `h-full`.

<CanvasWithCodePen of={GridSystemStories.Stretch} />

## Responsive

Tailored responsive options are at your disposal for customizations according to screen size.
Simply append a responsive breakpoint keyword with a dash, like `tablet:align-content-start`, to apply the corresponding responsive class.

<CssResponsiveTable all />

<CanvasWithCodePen of={GridSystemStories.Responsive} />

<Footer>
  <LinkCards>
    <LinkCard
      pageTitle="Foundation/Grid"
      description="The Helvetia Design System grid system is a fully responsive and 12 column layout system with six default breakpoints and five container layouts"
    />
    <LinkCard
      pageTitle="Tokens/All Tokens"
      description="Design tokens serve as the definitive source for naming and storing design choices."
    />
    <LinkCard
      pageTitle="CSS Utilities/Mixins/Breakpoints"
      description="To create a responsive layout you can either structure your app with the Sass mixins"
    />
  </LinkCards>
</Footer>
