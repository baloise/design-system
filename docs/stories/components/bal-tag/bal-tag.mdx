import { Canvas, Meta, ArgTypes, ColorPalette, ColorItem, Unstyled, Subtitle, Title, Markdown } from '@storybook/blocks'
import { Banner, Lead, VueFramework, AngularFramework, CodeSandbox, PlaygroundBar, StoryHeading } from '../../../.storybook/blocks'
import * as TagStories from './bal-tag.stories'

<Meta of={TagStories} />

<Banner of={TagStories} />

<Lead>**Tag** highlights a label for quick recognition.</Lead>

<Canvas of={TagStories.Primary} sourceState="shown"/>

<PlaygroundBar of={TagStories.Primary}></PlaygroundBar>

## Examples

<StoryHeading of={TagStories.TagGroup}></StoryHeading>

The tag component can be grouped alongside the `bal-tag-group` component, which offers optimal spacing options.

<Canvas of={TagStories.TagGroup} sourceState="shown"/>

<StoryHeading of={TagStories.Colors}></StoryHeading>

The tag component can be grouped alongside the `bal-tag-group` component, which offers optimal spacing options.

<Canvas of={TagStories.Colors}/>

<StoryHeading of={TagStories.TagCard}></StoryHeading>

Tags can be seamlessly integrated with a `bal-card` component. Additionally, you have the flexibility to position them either on the left or at the center.

<Canvas of={TagStories.TagCard}/>

## Component API

import api from './api.md?raw'

<Markdown>{api}</Markdown>

## Integration

import integration from '../../snippets/integration.md?raw'

<Markdown>{integration}</Markdown>

import theming from './theming.md?raw'

<Markdown>{theming}</Markdown>

import testing from './testing.md?raw'

<Markdown>{testing}</Markdown>
