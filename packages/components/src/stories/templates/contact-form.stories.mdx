import { Meta, Canvas, Source } from '@storybook/addon-docs'
import { contactFormTemplate } from './contact-form.template.ts'
import { html } from '../utils'
import dedent from 'ts-dedent'

<Meta
  title="Templates/Contact Form"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Contact Form

A basic template for a common form in our applications.

<Canvas withSource="none">
  <bal-doc-app>{html(contactFormTemplate)}</bal-doc-app>
</Canvas>

<Source
  language="html"
  code={dedent`
     ${contactFormTemplate}
  `}
/>
