import { Meta, Description, DocsContainer } from '@storybook/addon-docs'
import { BackToTop, TableOfContents } from 'hirsch-storybook-docs-toc'

import text_intro from './form_intro.md'
import text_angular from './form_angular.md'
import text_vue from './form_vue.md'

<Meta
  title="Development/Form"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
    },
    docs: {
      container: ({ context }) => {
        const framework = context && context.globals && context.globals.framework
        let markdown = text_angular
        if (framework === 'vue') {
          markdown = text_vue
        }
        return (
          <DocsContainer context={context}>
            <TableOfContents />
            <div className="my-doc-content">
              {<Description markdown={text_intro} />}
              {<Description markdown={markdown} />}
            </div>
            <BackToTop />
          </DocsContainer>
        )
      },
    },
  }}
/>

<bal-doc-github link="/stories/Development/form/form.stories.mdx"></bal-doc-github>
