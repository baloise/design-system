import { Story, Canvas, Description } from '@storybook/addon-docs'
import { typeahead, multiSelect } from './examples.docs.ts'
import { html } from '../../../stories/helpers/docs'
import readme from '../readme.md'
import selectOption from '../../bal-select-option/readme.md'

<span id="story--01-components-form-select--basic" style={{ opacity: 0 }}></span>

# Select

Buttons provide a clickable element, which can be used in forms, or anywhere that needs simple, standard button functionality. They may display text, icons, or both. Buttons can be styled with several attributes to look a specific way.

<Canvas className="bal-app has-visible-overflow" withSource="none">
  <Story id="01-components-form-select--basic" />
</Canvas>

## Typeahead

<Canvas className="bal-app has-visible-overflow" mdxSource={typeahead}>
  {html(typeahead)}
</Canvas>

## Multi-Select

<Canvas className="bal-app has-visible-overflow" mdxSource={multiSelect}>
  {html(multiSelect)}
</Canvas>

## Component Api

<Description markdown={readme} />
<Description markdown={selectOption} />

<br />

import testing from './testing.md'

<Description markdown={testing} />

import github from './github.md'

<Description markdown={github} />
