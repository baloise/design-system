import { Story, Canvas, Description } from '@storybook/addon-docs'

# Input

The input component is a wrapper to the HTML input element with custom styling and additional functionality. It accepts most of the same properties as the HTML input, but works great on desktop devices and integrates with the keyboard on mobile devices.

It is meant for text type inputs only, such as "text", "password", "email", "number", "search", "tel", and "url". It supports all standard text input events including keyup, keydown, keypress, and more.

> **Internationalization** To run this component in a different region then `CH` or to change the lauguage to `fr` follow thge documentation of [internationalization](?path=/docs/getting-started-internationalization--page).

<Canvas>
  <Story id="components-form-input--text-input" />
</Canvas>

### Angular Usage

To recover a string value use `toString()`.

```html
<bal-input
  [value]="myValue"
  (balInput)="setValue($event.detail?.toString())"
  (balChange)="setValue($event.detail?.toString())"
></bal-input>
```

```typescript
setValue(value: string | undefined) {
  if(value) { /* do stuff */ }
}
```

## Number Input

With number-input property your can only enter valid numbers, if you start with dot (.) zero will be automatically added in front of it.

With decimal property you can set how many decimal places you want and number will be formatted accordingly.

<Canvas>
  <Story id="components-form-input--number-input" />
</Canvas>

### Angular Usage

To recover a number value use `parseInt($event.detail, 10)` or `pareFloat($event.detail)`.

```html
<bal-input
  number-input
  decimal="2"
  [value]="myNumber"
  (balInput)="setNumber(pareFloat($event.detail))"
  (balChange)="setNumber(pareFloat($event.detail))"
></bal-input>
```

```typescript
setNumber(value: number | undefined) {
  if(value) { /* do stuff */ }
}
```

## Invalid Input

<Canvas>
  <Story id="components-form-input--invalid-input" />
</Canvas>

## Usage

import AngularLogo from '../../../../stories/assets/images/angular.svg'
import VueLogo from '../../../../stories/assets/images/vue.png'
import IconDocument from '../../../../../../icons/svg/icon-document.svg'
import IconAccount from '../../../../../../icons/svg/icon-account.svg'

### Form Validation

<div className="link-list">
  <a className="link-item" href="?path=/docs/design-form--page">
    <img src={IconDocument} alt="docu" />
    <span>
      <strong>Form</strong>
      Documentation how to build forms and how to use the components.
    </span>
  </a>
  <a className="link-item" href="?path=/story/design-common-templates--contact-form">
    <img src={IconAccount} alt="template" />
    <span>
      <strong>Template Contact Form</strong>A template with to illustrate the structure of a form.
    </span>
  </a>
  <a className="link-item" href="?path=/docs/getting-started-angular-usage-of-form-validation--page">
    <img src={AngularLogo} alt="angular" />
    <span>
      <strong>Angular</strong>
      Usage guide how to structure the form elements and to use form validation.
    </span>
  </a>
  <a className="link-item" href="?path=/docs/getting-started-vue-usage-of-form-validation--page">
    <img src={VueLogo} alt="vue" />
    <span>
      <strong>Vue</strong>
      Usage guide how to structure the form elements and to use form validation.
    </span>
  </a>
</div>

## Component Api

import readme from '../readme.md'

<Description markdown={readme} />

<br />

import testing from './testing.md'

<Description markdown={testing} />

<bal-doc-github link="/components/form/bal-input/stories/bal-input.docs.mdx"></bal-doc-github>
