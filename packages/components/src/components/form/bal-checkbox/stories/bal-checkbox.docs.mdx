import { Story, Canvas, Description } from '@storybook/addon-docs'
import { html } from '../../../../stories/utils'
import { Basic } from './bal-checkbox.stories'

<bal-doc-banner id="story--components-form-checkbox--basic" >Checkbox</bal-doc-banner>

<bal-doc-lead>
  <b>Checkboxes</b> allow users to select one or more items from a set. Checkboxes can turn one or more option(s) on or
  off.
</bal-doc-lead>

<Canvas withSource="open">
  <Story id="components-form-checkbox--basic" />
</Canvas>

<bal-doc-app>
  <bal-button-group class="mb-large" position="center">
    <a class="button is-primary" href="?path=/story/components-form-checkbox--basic">
      Go to playground (Canvas)
    </a>
    <bal-doc-code-sandbox
      framework="angular"
      template={Basic.parameters.docs.source.code}
      modules="checkbox"
    ></bal-doc-code-sandbox>
    <bal-doc-code-sandbox
      framework="html"
      template={Basic.parameters.docs.source.code}
      modules="checkbox"
    ></bal-doc-code-sandbox>
  </bal-button-group>
</bal-doc-app>

We introduced a new way of adding a options to the bal-checkbox-group
by introducing the `options` property in which options can be passed using a Javascript.

To use the `bal-checkbox-group` with options we also need to set `control` attribute to true.

```html
<bal-checkbox-group control [options]="options"></bal-checkbox-group>
```

```typescript
import { newBalCheckboxOption } from '@baloise/design-system-components'

@Component({
  selector: 'app-example-component',
  templateUrl: './example-component.component.html',
})
export class ExampleComponent {
  options = [
    newBalCheckboxOption({ value: '1', label: 'Label 1' }),
    newBalCheckboxOption({
      value: '2',
      label: () => 'Label with <a class="is-link" href="http://baloise.com">Link</a>',
    }),
  ]
}
```

## Examples

<span id="story--components-form-checkbox--field-control" style={{ opacity: 0 }}></span>

### Field Control

Combine the component with the `bal-field` components to add a label, validation message and more.

<Canvas>
  <Story id="components-form-checkbox--field-control" />
</Canvas>

<span id="story--components-form-checkbox--switch" style={{ opacity: 0 }}></span>

### Switch

Switches are used to toggle between exactly two states (like on and off).

<Canvas>
  <Story id="components-form-checkbox--switch" />
</Canvas>

<span id="story--components-form-checkbox--group" style={{ opacity: 0 }}></span>

### Group

<Canvas>
  <Story id="components-form-checkbox--group" />
</Canvas>

<span id="story--components-form-checkbox--vertical" style={{ opacity: 0 }}></span>

#### Vertical Group

<Canvas>
  <Story id="components-form-checkbox--vertical" />
</Canvas>

<span id="story--components-form-checkbox--filter-buttons" style={{ opacity: 0 }}></span>

#### Filter Buttons

<Canvas>
  <Story id="components-form-checkbox--filter-buttons" />
</Canvas>

<span id="story--components-form-checkbox--checkbox-button" style={{ opacity: 0 }}></span>

### Checkbox Button

The content can be created flexible since we use the components [stack component](?path=/docs/components-layout-stack--basic) and [content component](?path=/docs/components-layout-content--basic).

The states invalid and disabled are also supported.

<Canvas>
  <Story id="components-form-checkbox--checkbox-button" />
</Canvas>

<span id="story--components-form-checkbox--checkbox-button-grid" style={{ opacity: 0 }}></span>

### Checkbox Button Grid

The checkbox buttons are have a built in grid system. Just add the props `columns`, `columns-mobile` or `columns-tablet` to the `bal-radio-group`.

By default on mobile we only support one item per line. To overwrite this use the prop `columns-mobile`.

<Canvas>
  <Story id="components-form-checkbox--checkbox-button-grid" />
</Canvas>

## Component Api

import readme from '../readme.md'
import checkboxGroup from '../bal-checkbox-group/readme.md'
import checkboxButton from '../bal-checkbox-button/readme.md'

<Description markdown={readme} />
<Description markdown={checkboxGroup} />
<Description markdown={checkboxButton} />

<br />

## Integration

This documentation explains how to implement and use Baloise Design System components across different technologies.

<a class="button is-primary" href="?path=/docs/development-components--page">
  Go to component usage
</a>

import theming from './theming.md'

<Description markdown={theming} />

import testing from './testing.md'

<Description markdown={testing} />

<bal-doc-github link="/components/form/bal-checkbox/stories/bal-checkbox.docs.mdx"></bal-doc-github>
