import { Story, Canvas, Description } from '@storybook/addon-docs'
import { html } from '../../../stories/utils'
import { Basic } from './bal-accordion.stories'

<bal-doc-banner id="story--components-accordion--basic">Accordion</bal-doc-banner>

<bal-doc-lead>
  <b>Accordions</b> put users in control of showing or hiding content. Accordions also help us organize information to
  keep screens less cluttered so that users can accomplish tasks in short, intuitive steps. And accordions can help
  users find content they need. The component can be used standalone, in combination or inside{' '}
  <a href="?path=/docs/components-card--basic">BalCard</a>.
</bal-doc-lead>

The accordion is build on 3 child components:

- `bal-accordion-summary` defines the visible header of the accordion and can also act as a trigger.
- `bal-accordion-trigger` shows and hides the details of the accordion.
- `bal-accordion-details` is the content to be hidden by default.

<Canvas withSource="open">
  <Story id="components-accordion--basic" />
</Canvas>

import exampleHtml from './usage/angular/example_component_html.md'
import exampleTs from './usage/angular/example_component_ts.md'
import reactExampleTsx from './usage/react/example_tsx.md'
import htmlExampleHtml from './usage/html/example_html.md'
import htmlExampleTs from './usage/html/example_ts.md'

<bal-doc-app>
  <bal-button-group class="mb-large" position="center">
    <a class="button is-primary" href="?path=/story/components-accordion--basic">
      Go to playground (Canvas)
    </a>
    <bal-doc-code-sandbox framework="angular" template={exampleHtml} component={exampleTs}></bal-doc-code-sandbox>
    <bal-doc-code-sandbox framework="html" template={htmlExampleHtml} component={htmlExampleTs}></bal-doc-code-sandbox>
    <bal-doc-code-sandbox framework="react" component={reactExampleTsx}></bal-doc-code-sandbox>
  </bal-button-group>
</bal-doc-app>

> **TIP**
>
> Check out the layout components [bal-stack](?path=/docs/components-layout-stack--basic) and [bal-content](?path=/docs/components-layout-content--basic) of the above example.

## Examples

<span id="story--components-accordion--button-accordion" style={{ opacity: 0 }}></span>

### Button Accordion

To change the label use the properties `open-label` & `close-label`. The icon can be changed with the properties `open-icon` and `close-icon`.

<Canvas>
  <Story id="components-accordion--button-accordion" />
</Canvas>

<span id="story--components-accordion---old-version" style={{ opacity: 0 }}></span>

### Old Version

The old version was created / designed in the previous style guide. We learned that we need a more flexible solution,
therefore we restructured the accordion.
We keep the old version intact, but highly recommend to use the new version above.

<Canvas>
  <Story id="components-accordion--old-version" />
</Canvas>

## Component Api

import readme from '../readme.md'
import readmeSummary from '../bal-accordion-summary/readme.md'
import readmeTrigger from '../bal-accordion-trigger/readme.md'
import readmeDetails from '../bal-accordion-details/readme.md'

<Description markdown={readme} />
<Description markdown={readmeSummary} />
<Description markdown={readmeTrigger} />
<Description markdown={readmeDetails} />

<br />

## Integration

For integration, properties, event and methods, please see Component Usage.
This documentation explains how to implement and use Baloise Design System components across different technologies like Angular, React, Vue or Vanilla JavaScript.

<a class="button is-primary" href="?path=/docs/development-components--page">
  Go to Component Usage
</a>

import testing from './testing.md'

<Description markdown={testing} />

<bal-doc-github link="/components/bal-accordion/stories/bal-accordion.docs.mdx"></bal-doc-github>
