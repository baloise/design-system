import { Story, Canvas, Description } from '@storybook/addon-docs'
import { html } from '../../../stories/utils'
import readme from '../readme.md'
import { Basic } from './bal-tooltip.stories'

<bal-doc-banner id="story--components-tooltip--basic">tooltip</bal-doc-banner>

<bal-doc-lead>
  <b>Tooltips</b> provide informative text that is displayed when users hover over, focus on, or tap an element,
  offering additional context or details. When triggered, Tooltips present a textual label that identifies an element,
  providing information about its purpose or function.
</bal-doc-lead>

<Canvas withSource="open" className="has-visible-overflow">
  <Story id="components-tooltip--basic" />
</Canvas>

<bal-doc-app>
  <bal-button-group class="mb-large" position="center">
    <a class="button is-primary" href="?path=/story/components-tooltip--basic">
      Go to playground (Canvas)
    </a>
    <bal-doc-code-sandbox framework="angular" template={Basic.parameters.docs.source.code}></bal-doc-code-sandbox>
    <bal-doc-code-sandbox framework="html" template={Basic.parameters.docs.source.code}></bal-doc-code-sandbox>
  </bal-button-group>
</bal-doc-app>

## Usage

To link the tooltip component to a trigger element, you need to assign a reference to your tooltip component and add the bal-tooltip attribute to the trigger element, as shown in the example below:

```html
<bal-button bal-tooltip="my-tooltip">Click me</bal-button>
<bal-tooltip label="tooltip Label" reference="my-tooltip">Tooltip content</bal-tooltip>
```

In this example, the bal-button element serves as the trigger element for the tooltip component.
It includes the bal-tooltip attribute, which references the reference of the tooltip component, "my-tooltip".

The actual content of the tooltip is defined within the bal-tooltip element.
It includes a label attribute, "Tooltip Label", for accessibility purposes and to show a heading.

### Positioned tooltips

The Tooltip offers a selection of 12 placement options, each without directional arrows. Instead, they utilize motion originating from the source element to indicate the intended direction.  
You can use the `placement` property to specify the position of the Tooltip. This property allows you to choose from a variety of options to determine where the Tooltip should appear in relation to its source element. Here's an example:

```html
<bal-button bal-tooltip="my-tooltip">Click me</bal-button>
<bal-tooltip label="tooltip Label" reference="my-tooltip" placement="right">Tooltip content</bal-tooltip>
```

### Arrow tooltips

The `arrow` prop can be utilized to include an arrow in the Tooltip, visually indicating the associated element to which it pertains. This arrow serves as a directional cue, providing a clear visual connection between the Tooltip and its relevant element.

```html
<bal-button bal-tooltip="my-tooltip">Click me</bal-button>
<bal-tooltip label="tooltip Label" reference="my-tooltip" arrow>Tooltip content</bal-tooltip>
```

## Component Api

<Description markdown={readme} />

<br />

## Integration

This documentation explains how to implement and use Baloise Design System components across different technologies.

<a class="button is-primary" href="?path=/docs/development-components--page">
  Go to component usage
</a>

import theming from './theming.md'

<Description markdown={theming} />

import testing from './testing.md'

<Description markdown={testing} />

<bal-doc-github link="/components/bal-tooltip/stories/bal-tooltip.docs.mdx"></bal-doc-github>
