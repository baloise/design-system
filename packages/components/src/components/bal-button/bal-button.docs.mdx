import { Story, Preview } from '@storybook/addon-docs'
import { html } from '../../stories/helpers/docs'
import { basic, links, colors, expanded, icons, square, buttonLink, group } from './bal-button.docs.ts'

# Button

Buttons provide a clickable element, which can be used in forms, or anywhere that needs simple, standard button functionality. They may display text, icons, or both. Buttons can be styled with several attributes to look a specific way.

<Preview>
  <Story id="01-atoms-button--primary" />
</Preview>

## Examples

### Basic

<Preview mdxSource={basic}>{html(basic)}</Preview>

### Links

<Preview mdxSource={links}>{html(links)}</Preview>

### Colors

<Preview mdxSource={colors}>{html(colors)}</Preview>

### Expanded

<Preview mdxSource={expanded}>{html(expanded)}</Preview>

### With Icons

<Preview mdxSource={icons}>{html(icons)}</Preview>

### Square

<Preview mdxSource={square}>{html(square)}</Preview>

### Button Link

<Preview mdxSource={buttonLink}>{html(buttonLink)}</Preview>

### Button Group

The .bal-buttons css class helps to place button groups together. The buttons stretche on mobile devices according to their css class like .is-full , .is-half , .is-one-third or .is-two-thirds .

<Preview mdxSource={group}>{html(group)}</Preview>
