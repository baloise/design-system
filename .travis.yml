os:
  - linux

language: node_js

node_js:
  - 10

script:
  - npm run build
  - npm run test

jobs:
  include:
    - stage: release
      node_js: lts/*
      script:
        - npm run semantic-release
