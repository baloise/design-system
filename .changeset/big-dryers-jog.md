---
'@baloise/ds-core': major
'@baloise/ds-angular': major
'@baloise/ds-react': major
---

**core/angular/react**: Renamed `ds-input-slider` to `ds-slider` (tag, class, and all exported types) to match common design-system naming (no native `<input>` element) and drop the redundant 'Input' prefix.
