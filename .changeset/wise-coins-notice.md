---
'@baloise/ds-react': patch
---

**react**: Fix ds-input-phone flag rendering crash under Vite by setting Stencil's resourcesUrl via the dist-custom-elements entry point, anchored to respect a subpath deployment
