---
'@baloise/ds-core': patch
---

**core**: Fix broken --ds-alias-text-family-body reference (should be --ds-alias-text-family-text), which left description/body text falling back to the browser default font in list, tabs, hint, input-stepper, notification, snackbar, toast, file-upload, and input-slider
