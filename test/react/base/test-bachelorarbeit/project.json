{
  "name": "test-bachelorarbeit",
  "version": "1.0.0",
  "private": true,
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist"
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "options": {
        "buildTarget": "test-bachelorarbeit:build"
      }
    }
  },
  "tags": ["type:app"]
}
