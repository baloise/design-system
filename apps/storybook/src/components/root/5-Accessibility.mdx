import { Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  A11yGuidelines,
  Banner,
  BannerTabs,
  Footer,
} from '../../../.storybook/blocks'
import * as RootStories from './root.stories'
import { ROOT_DOC_CONFIG, getRootTabs } from './root.doc-config'

<Meta title="Components/Root/Accessibility" name="Accessibility" />
<Banner label={'Accessibility'} section={ROOT_DOC_CONFIG.section} color={ROOT_DOC_CONFIG.color} />
<BannerTabs
  of={RootStories}
  tabs={getRootTabs('accessibility')}
/>

## Guidelines

<A11yGuidelines
  items={[
    {
      type: 'do',
      title: 'Use Semantic Landmarks',
      content: (
        <p style={{ margin: '0.5rem 0 0 0' }}>
          Use <code>role="banner"</code>, <code>role="main"</code>, and <code>role="contentinfo"</code> for navigation and content areas. Screen readers need structure.
        </p>
      ),
    },
    {
      type: 'do',
      title: 'Skip Links',
      content: (
        <p style={{ margin: '0.5rem 0 0 0' }}>
          Include skip navigation links to jump directly to main content. Helps keyboard users bypass repeated navigation.
        </p>
      ),
    },
    {
      type: 'do',
      title: 'Focus Management',
      content: (
        <p style={{ margin: '0.5rem 0 0 0' }}>
          Manage focus appropriately on page transitions and navigation. Announce page changes to screen readers.
        </p>
      ),
    },
    {
      type: 'do',
      title: 'Keyboard Navigation',
      content: (
        <p style={{ margin: '0.5rem 0 0 0' }}>
          Ensure all app navigation is keyboard accessible. Tab through headers, nav, content, and footer.
        </p>
      ),
    },
    {
      type: 'dont',
      title: 'No Semantic Structure',
      content: (
        <p style={{ margin: '0.5rem 0 0 0' }}>
          Don't use generic divs without landmarks. Screen readers need to understand app structure.
        </p>
      ),
    },
    {
      type: 'dont',
      title: 'Trapped Focus',
      content: (
        <p style={{ margin: '0.5rem 0 0 0' }}>
          Don't trap keyboard focus. Users should be able to navigate in and out of app regions.
        </p>
      ),
    },
  ]}
/>

## WCAG 2.2 AA Compliance

The root component supports patterns that meet WCAG 2.2 Level AA standards:

- **Semantic Landmarks (1.3.1)** — Uses role attributes for page regions
- **Navigable Structure (2.4.1)** — Skip links and landmarks for navigation
- **Keyboard Navigation (2.1.1)** — Tab navigates chrome and content
- **Focus Management (2.4.3)** — Focus visible and managed appropriately
- **Page Structure (2.4.6)** — Clear visual hierarchy and organization

<Footer />
