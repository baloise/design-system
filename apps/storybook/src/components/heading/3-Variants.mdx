import { Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  CanvasTabs,
  CanvasWithCodePen,
  Footer,
  StoryHeading,
} from '../../../.storybook/blocks'
import * as HeadingStories from './heading.stories'
import { HEADING_DOC_CONFIG, getHeadingTabs } from './heading.doc-config'

<Meta title="Components/Heading/Variants/Overview" />
<StoryHeading of={HeadingStories.Basic} hidden></StoryHeading>
<Banner label={'Variants'} section={HEADING_DOC_CONFIG.section} color={HEADING_DOC_CONFIG.color} />
<BannerTabs
  of={HeadingStories}
  tabs={getHeadingTabs('variants')}
/>

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.Levels}></StoryHeading>

Headings with different levels (H1 to H6) to establish a clear content hierarchy and improve readability.

<CanvasWithCodePen of={HeadingStories.Levels} sourceState="hidden" />

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.Sizes}></StoryHeading>

Headings with different sizes to establish a clear content hierarchy and improve readability.

<CanvasWithCodePen of={HeadingStories.Sizes} sourceState="hidden" />

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.Colors}></StoryHeading>

Headings support multiple colors for visual emphasis and theming. The default color adapts to background context with the `inverted` prop.

<CanvasWithCodePen of={HeadingStories.Colors} sourceState="hidden" />

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.NoWrap}></StoryHeading>

Headings can be set to not wrap, truncating overflow text with an ellipsis to maintain layout integrity.

<CanvasWithCodePen of={HeadingStories.NoWrap} sourceState="hidden" />

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.Spacing}></StoryHeading>

Headings can have custom spacing to adjust the visual rhythm and layout consistency.

<CanvasWithCodePen of={HeadingStories.Spacing} sourceState="hidden" />

{/* ------------------------------------------------------ */}

<StoryHeading of={HeadingStories.AutoLevel}></StoryHeading>

Headings can automatically adjust their level based on the surrounding context to maintain a consistent hierarchy.

<CanvasWithCodePen of={HeadingStories.AutoLevel} sourceState="hidden" />

<Footer />
