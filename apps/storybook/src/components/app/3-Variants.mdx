import { Markdown, Meta } from '@storybook/addon-docs/blocks'
import { Banner, BannerTabs, CanvasWithCodePen, Footer, StoryHeading } from '../../../.storybook/blocks'
import * as AppStories from './app.stories'
import { APP_DOC_CONFIG, getAppTabs } from './app.doc-config'

<Meta title="Components/App/Variants/Overview" name="Overview" />

<StoryHeading of={AppStories.Basic} hidden></StoryHeading>

<Banner label={'Variants'} section={APP_DOC_CONFIG.section} color={APP_DOC_CONFIG.color} />

<BannerTabs of={AppStories} tabs={getAppTabs('variants')} />

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={AppStories.Basic}></StoryHeading>

The default app shell — header, main content, and footer.

<CanvasWithCodePen of={AppStories.Basic} />

{/* ------------------------------------------------------ */}

<StoryHeading of={AppStories.WithConfig}></StoryHeading>

Set `brand`, `region`, `language`, `allowed-languages`, and `fallback-language` on `ds-app` to configure every design system component nested inside it. Here `language="it"` makes the `ds-close` button render its label in Italian ("Chiudi") instead of the default.

<CanvasWithCodePen of={AppStories.WithConfig} />

<Footer />
