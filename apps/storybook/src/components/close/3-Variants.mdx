import { Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  Banner,
  BannerTabs,
  CanvasWithCodePen,
  Footer,
  StoryHeading,
} from '../../../.storybook/blocks'
import * as CloseStories from './close.stories'
import { CLOSE_DOC_CONFIG, getCloseTabs } from './close.doc-config'

<Meta title="Components/Close/Variants/Overview" />
<StoryHeading of={CloseStories.Basic} hidden></StoryHeading>
<Banner label={'Variants'} section={CLOSE_DOC_CONFIG.section} color={CLOSE_DOC_CONFIG.color} />
<BannerTabs
  of={CloseStories}
  tabs={getCloseTabs('variants')}
/>

{/* STORIES */}
{/* ------------------------------------------------------ */}

<StoryHeading of={CloseStories.Basic}></StoryHeading>

The default icon-only close control. Use it inside notifications, alerts, or other dismissible UI where a compact, borderless button is expected.

<CanvasWithCodePen of={CloseStories.Basic} />

{/* ------------------------------------------------------ */}

<StoryHeading of={CloseStories.AsButton}></StoryHeading>

Set the `button` prop to render the close control as a standalone `ds-button`. Use this when the close action needs to stand on its own, for example outside of a notification or alert.

<CanvasWithCodePen of={CloseStories.AsButton} />

<Footer />
