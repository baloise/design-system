import { Markdown, Meta } from '@storybook/addon-docs/blocks'
import {
  AngularFramework,
  Banner,
  Footer,
  HTMLFramework,
  InfoQuote,
  Lead,
  LinkCard,
  LinkCards,
  PackageTable,
  ReactFramework,
  WarningQuote,
} from '../../../.storybook/blocks'

<Meta title="Development/Component" name="Component" />

<Banner label="Component" section="Development" />

<Lead>
  By using the standardized web platform APIs and Web Components the Design System achieved that the components can be
  used across any framework or no framework at all
</Lead>

## Component Usage

The code is designed to improve the performance of the application by lazy loading each component to the browser
only when it is needed. Additionally, sub-components will be automatically resolved and loaded as well. To ensure
that the Design System is initialized correctly, it is crucial that the application is wrapped in the ds-root component

<AngularFramework>

**app.config.ts**

```ts
import { ApplicationConfig } from '@angular/core'
import { provideDesignSystem } from '@baloise/ds-angular'

export const appConfig: ApplicationConfig = {
  providers: [provideDesignSystem()],
}
```

**main.ts**

```ts
import { bootstrapApplication } from '@angular/platform-browser'

import { App } from './app/app'
import { appConfig } from './app/app.config'

bootstrapApplication(App, appConfig).catch(err => console.error(err))
```

**app.ts**

```ts
import { Component } from '@angular/core'
import { DsHeading, DsRoot } from '@baloise/ds-angular'

@Component({
  selector: 'app-root',
  imports: [DsRoot, DsHeading],
  templateUrl: './app.html',
})
export class App {}
```

**app.html**

```html
<ds-root>
  <main class="ds-container">
    <ds-heading>Hello World</ds-heading>
  </main>
</ds-root>
```

</AngularFramework>

{/* ################################################## */}
{/* -------------------------------------------------- */}
{/* HTML  -------------------------------------------- */}
{/* -------------------------------------------------- */}
{/* ################################################## */}

<HTMLFramework>

```html
<ds-root>
  <main class="ds-container">
    <ds-heading>Hello World</ds-heading>
  <main>
</ds-root>
```

### When is the component ready

The [CustomElementRegistry](https://developer.mozilla.org/en-US/docs/Web/API/CustomElementRegistry) interface provides methods for registering custom elements and
querying registered elements. To get an instance of it, use the window.customElements property.

The [whenDefined](https://developer.mozilla.org/en-US/docs/Web/API/CustomElementRegistry/whenDefined) method of the CustomElementRegistry interface returns a Promise that
resolves when the named element is defined.

```js
window.customElements.whenDefined('ds-button').then(() => {
  console.log('Helvetia Design System Button is defined!')
})
```

The `whenDefined()` tells when the component is added to the browser.
Additionally to know when the component has fully rendered ou could use `componentOnReady()` that the components provide.

```js
await customElements.whenDefined('ds-button')
await document.querySelector('ds-button').componentOnReady()
```

</HTMLFramework>

{/* ################################################## */}
{/* -------------------------------------------------- */}
{/* React  ------------------------------------------- */}
{/* -------------------------------------------------- */}
{/* ################################################## */}

<ReactFramework>

Import the components that are used inside the template and that is it.

**App.tsx**

```tsx
import React from "react";
import { DsRootProvider, DsHeading } from "@baloise/ds-react";

function App() {
  return (
    <DsRootProvider>
      <main className="ds-container">
        <DsHeading>Hello World</DsHeading>
      </main>
    </DsRootProvider>
  );
}

export default App;
```

</ReactFramework>

{/* ############################################################################################ */}
{/* Properties */}
{/* ############################################################################################ */}

## Properties

Properties are custom attributes/properties exposed publicly on an HTML element. They allow developers
to pass data to a component to render or otherwise use. Like to set a value for a input or change the
color of a button.

To showcase property interaction, an attempt is made to modify the color property of the button component.

```html
<!-- with color secondary -->
<ds-button color="secondary">My secondary button</ds-button>
```

If the property is of type `boolean` it is enough to just set the property without a value:

```html
<!-- disabled button -->
<ds-button disabled>My disabled button</ds-button>
```

<AngularFramework>

### Bind properties

**app.component.ts**

```ts
import { Component } from '@angular/core'

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
})
export class AppComponent {
  buttonColor = 'secondary'
}
```

**app.component.html**

```html
<ds-button color="secondary">My secondary button</ds-button>

<!-- To bind a value use the [] around the prop -->

<ds-button [color]="buttonColor">My secondary button</ds-button>
```

</AngularFramework>

{/* ################################################## */}
{/* -------------------------------------------------- */}
{/* HTML  -------------------------------------------- */}
{/* -------------------------------------------------- */}
{/* ################################################## */}

<HTMLFramework>

### Bind properties

```html
<ds-button color="secondary">My secondary button</ds-button>
<ds-button id="my-secondary-button">My secondary button</ds-button>

<script>
  const buttonElement = document.querySelector('#my-secondary-button')
  buttonElement.color = 'secondary'
</script>
```

</HTMLFramework>

{/* ################################################## */}
{/* -------------------------------------------------- */}
{/* React  ------------------------------------------- */}
{/* -------------------------------------------------- */}
{/* ################################################## */}

<ReactFramework>

### Bind properties

```tsx
import { useState } from 'react'
import { DsButton } from '@baloise/ds-react'

function App() {
  const [color, setColor] = useState<'info' | 'secondary'>('info')

  return (
    <>
      <DsButton color="info">My info button</DsButton>
      <DsButton color={color} onDsClick={() => setColor('secondary')}>
        My secondary button
      </DsButton>
    </>
  )
}

export default App
```

</ReactFramework>

{/* ############################################################################################ */}
{/* Events */}
{/* ############################################################################################ */}

## Events

The most of the Helvetia Design System components can emit data and events using the Custom events.

To demonstrate how to interact with events let's try to listen to the input event of an input component.

```html
<!-- simple text input -->
<ds-input placeholder="Tell us your name"></ds-input>
```

<AngularFramework>

In Angular the events are written like that `(dsEventName)="yourComponentHandler($event.detail)"`.

`$event.detail` returns the main value of the event.

**app.component.html**

```html
<ds-input placeholder="Tell us your name" [value]="myName" (dsInput)="updateName($event.detail)"></ds-input>
<ds-text>My name is {{ myName }}</ds-text>
```

**app.component.ts**

```ts
import { Component } from '@angular/core'
import type { InputInputDetail } from '@baloise/ds-core'

@Component({
  selector: 'app-event',
  templateUrl: './event.component.html',
})
export class EventComponent {
  myName?: string

  updateName(value: InputInputDetail) {
    this.myName = value
  }
}
```

</AngularFramework>

{/* ################################################## */}
{/* -------------------------------------------------- */}
{/* HTML  -------------------------------------------- */}
{/* -------------------------------------------------- */}
{/* ################################################## */}

<HTMLFramework>

Since the components are using [dom events](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Building_blocks/Events)
they can easily listen to them with `addEventListener`.

```html
<ds-input id="my-input" placeholder="Tell us your name"></ds-input>
<ds-text id="my-name"></ds-text>

<script>
  const myInput = document.getElementById('my-input')
  const myName = document.getElementById('my-name')

  myInput.addEventListener('dsInput', event => {
    myName.textNode = event.detail
  })
</script>
```

</HTMLFramework>

{/* ################################################## */}
{/* -------------------------------------------------- */}
{/* React  ------------------------------------------- */}
{/* -------------------------------------------------- */}
{/* ################################################## */}

<ReactFramework>

Each event in React is prefixed with `on`. `dsInput` becomes `onDsInput`.
`$event.detail` returns the main value of the event.

```tsx
import { useState } from 'react'
import { DsInput, DsText } from '@baloise/ds-react'

function EventComponent() {
  const [myName, setMyName] = useState<string | undefined>()

  return (
    <div>
      <DsInput placeholder="Tell us your name" value={myName} onDsInput={e => setMyName(e.detail)}></DsInput>
      <DsText>My name is {myName}</DsText>
    </div>
  )
}

export default EventComponent
```

</ReactFramework>

{/* ############################################################################################ */}
{/* Methods */}
{/* ############################################################################################ */}

## Methods

Some of the Helvetia Design System components offering methods to setFocus or returning native HTML elements.

To demonstrate the usage of the methods let's set the value of the ds-select to tennis. Options are set via the
`options` property as plain JS objects rather than `ds-select-option` markup.

```html
<ds-select placeholder="Choose your hobby"></ds-select>
```

<AngularFramework>

To create a reference to a component use the property decorator [@ViewChild](https://angular.io/api/core/ViewChild).
Mark the component in the template with `#yourReferenceName` and link it to the `@ViewChild` in the component class.
To access the method of the component use the reference directly.

**app.component.html**

```html
<ds-select #mySelect placeholder="Choose your hobby" [options]="hobbyOptions"></ds-select>
<ds-button class="mt-base" (click)="chooseTennis()">Choose Tennis</ds-button>
```

**app.component.ts**

```typescript
import { Component, ViewChild } from '@angular/core'
import type { Components } from '@baloise/ds-core'
import { ProxyComponent } from '@baloise/ds-angular'

@Component({
  selector: 'app-method',
  templateUrl: './method.component.html',
})
export class MethodComponent {
  @ViewChild('mySelect') mySelect!: ProxyComponent<Components.DsSelect>

  hobbyOptions = [
    { label: 'Tennis', value: 'tennis' },
    { label: 'Soccer', value: 'soccer' },
    { label: 'Other', value: 'other' },
  ]

  chooseTennis(): void {
    this.mySelect.el.select('tennis')
  }
}
```

</AngularFramework>

{/* ################################################## */}
{/* -------------------------------------------------- */}
{/* HTML  -------------------------------------------- */}
{/* -------------------------------------------------- */}
{/* ################################################## */}

<HTMLFramework>

```html
<ds-select id="hobby-select" placeholder="Choose your hobby"></ds-select>

<script>
  const hobbySelect = document.querySelector('#hobby-select')
  hobbySelect.options = [
    { label: 'Tennis', value: 'tennis' },
    { label: 'Soccer', value: 'soccer' },
    { label: 'Other', value: 'other' },
  ]

  ;(async () => {
    await customElements.whenDefined('ds-select')
    await hobbySelect.select('tennis')
  })()
</script>
```

</HTMLFramework>

{/* ################################################## */}
{/* -------------------------------------------------- */}
{/* React  ------------------------------------------- */}
{/* -------------------------------------------------- */}
{/* ################################################## */}

<ReactFramework>

```tsx
import { useRef } from 'react'
import { DsSelect, DsButton } from '@baloise/ds-react'

const hobbyOptions = [
  { label: 'Tennis', value: 'tennis' },
  { label: 'Soccer', value: 'soccer' },
  { label: 'Other', value: 'other' },
]

function MethodComponent() {
  const selectRef = useRef<HTMLDsSelectElement>(null)

  function chooseTennis() {
    selectRef.current?.select('tennis')
  }

  return (
    <div>
      <DsSelect ref={selectRef} placeholder="Choose your hobby" options={hobbyOptions} />
      <DsButton className="mt-base" onDsClick={chooseTennis}>
        Choose Tennis
      </DsButton>
    </div>
  )
}

export default MethodComponent
```

</ReactFramework>

<Footer />
