import { Meta } from '@storybook/addon-docs/blocks'
import { Banner, Code, Footer, InfoQuote, Lead, LinkCard, LinkCards } from '../../../.storybook/blocks'

<Meta title="Development/Theming" name="Theming" />

<Banner label="Theming" section="Development" />

<Lead>
  Adjust the Design System to fit your brand — override a single component, or switch your whole
  application to a different predefined brand.
</Lead>

## CSS Variables

Every value in the Design System — colors, spacing, typography, and each component's own styling —
is exposed as a [CSS Variable](https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties).


A CSS Variable is defined once and referenced everywhere, so overriding it in one place cascades
everywhere it's used — no build step or preprocessor required.

## Customize Components

### Globally

Override a design token in `:root` and every component using it picks up the new value.

<Code
  language="css"
  code={`
:root {
  --ds-global-color-primary: pink;
  --ds-global-font-family-heading: 'Roboto', sans-serif;
}
`}
/>

### By Component

Every component also exposes its own public variables. Target its selector to override just that
component, everywhere it appears:

<Code
  language="css"
  code={`
ds-badge {
  --ds-badge-background: aqua;
}
`}
/>

<br />

Or scope it to a single instance with a class:

<br />

<Code
  language="css"
  code={`
.my-badge {
  --ds-badge-background: lime;
}
`}
/>

<InfoQuote>
  See [Component Variables](?path=/docs/tokens-component-variables--documentation) for the full
  private/public/modifier/token variable cascade each component follows.
</InfoQuote>

## Predefined Brand Themes

Each brand (e.g. Orange Vacations) ships as two complete, standalone token stylesheets from
`@baloise/ds-tokens` — not a diff against Base, the full token set either way. Pick the one that
matches how your app switches (or doesn't switch) brands:

- **`css/<brand>`** (e.g. `css/orange-vacations`) — scoped to `:host, :root`. Use this when your whole app is
  committed to one brand; import it instead of `css/base`, nothing else needed.
- **`css/<brand>-override`** (e.g. `css/orange-vacations-override`) — scoped to
  `[data-theme="<brand>"], :host([data-theme="<brand>"])`. Use this to switch brands at runtime,
  scoped to whichever element carries the `data-theme` attribute — the rest of the page keeps
  whatever tokens it already has.

### Whole app, one brand

<br />

<Code
  language="css"
  code={`
@import '@baloise/ds-tokens/css/orange-vacations';
`}
/>

Load this instead of `@baloise/ds-tokens/css/base` (see
[Styles](?path=/docs/development-styles--documentation)) — don't load both.

### Scoped to an element

**1. Import the brand's override stylesheet**

<br />

<Code
  language="css"
  code={`
@import '@baloise/ds-tokens/css/orange-vacations-override';
`}
/>

<br />

**2. Activate it on any element**

Set `data-theme="<brand>"` on the element you want scoped to that brand — a `<div>`, not just the
root:

<br />

<Code
  language="html"
  noPreview
  code={`
<div data-theme="orange-vacations">
`}
/>

<InfoQuote>
  Brand themes are actively being built out and aren't all published yet — check with the Design
  System team for which brands are currently available to consume, and under which package path.
</InfoQuote>

### Requesting a New Brand

Need a brand that isn't available yet? Open a
[Theme Report](https://github.com/baloise/design-system/issues/new?template=theme_report.yml) —
include a link to the brand's guidelines (colors, typography, logo) if you have one, so the
Design System team can scope the token overrides.

<Footer>
  <LinkCards>
    <LinkCard
      pageTitle="Development/Styles"
      description="Install and import the foundation, component, and utility styles the theming system builds on."
    />
    <LinkCard
      pageTitle="Tokens/Component Variables"
      description="The private/public/modifier/token CSS variable cascade every component follows."
    />
  </LinkCards>
</Footer>
