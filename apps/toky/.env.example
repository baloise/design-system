# GitHub token used server-side to read/write *.tokens.json in the design-system repo
# and open pull requests. Needs Contents: read+write and Pull requests: write (a classic
# PAT with the "repo" scope also works). Interim: any personal access token with that
# access. Long-term: the dedicated bot user's fine-grained PAT (see toky.md's Auth &
# permissions section).
TOKY_GITHUB_TOKEN=

# Branch to read tokens from and open pull requests against. Defaults to "next" if unset.
TOKY_GITHUB_REF=
